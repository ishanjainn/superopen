name: CI / CLI

# Go tests + cross-compile smoke + plugin-tree drift check.
# PRs get the same signal as release.

on:
  workflow_call:
    inputs:
      ref:
        description: Commit SHA to validate
        required: false
        default: ""
        type: string
  push:
    branches: ["main"]
    paths:
      - "cmd/**"
      - "internal/**"
      - "plugins/**"
      - ".claude-plugin/**"
      - "scripts/**"
      - "go.mod"
      - "go.sum"
      - "Makefile"
      - ".github/workflows/ci-cli.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "cmd/**"
      - "internal/**"
      - "plugins/**"
      - ".claude-plugin/**"
      - "scripts/**"
      - "go.mod"
      - "go.sum"
      - "Makefile"
      - ".github/workflows/ci-cli.yml"

permissions:
  contents: read

jobs:
  test:
    name: Test / Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.mod
          check-latest: true
      - name: go mod download
        run: go mod download
      - name: go build
        run: go build -trimpath ./...
      - name: go vet
        run: go vet ./...
      - name: go test
        # -race for sessionstate / OTLP / projects registry concurrency.
        # -count=1 disables the test cache.
        # -timeout 30m: graph/engine race + wazero grammar compiles exceed the
        # default 10m package timeout on slower runners.
        run: go test -race -timeout 30m -count=1 ./...

  test-native:
    name: Test / Native Tree-sitter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.mod
          check-latest: true
      - name: go test tsnative
        env:
          CGO_ENABLED: "1"
        run: go test -tags tsnative,sqlite_fts5 -timeout 30m -count=1 ./internal/graph/engine ./internal/graph/engine/tsnative

  cross-compile:
    name: Build / ${{ matrix.os }}-${{ matrix.arch }}
    runs-on: ubuntu-latest
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: windows, arch: amd64 }
          - { os: darwin, arch: arm64 }
          - { os: linux, arch: amd64 }
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.mod
          check-latest: true
      - name: Build
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 0
        run: go build -trimpath -o /dev/null ./cmd/so

  plugin-sync:
    name: Validate / Plugin sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
      - name: Sync into a temp dir and diff
        run: |
          set -euo pipefail
          BEFORE=$(mktemp -d)
          cp -R internal/agent/install/marketplace/. "$BEFORE/"
          bash scripts/sync-plugins.sh
          if ! diff -ru "$BEFORE" internal/agent/install/marketplace; then
            echo
            echo "::error::plugins/ or .claude-plugin/ has drifted from internal/agent/install/marketplace/."
            echo "Run \`bash scripts/sync-plugins.sh\` locally and commit the result."
            exit 1
          fi
          echo "embedded marketplace tree is in sync"
