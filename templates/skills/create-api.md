# Create API

Use this skill when adding a REST/HTTP endpoint.

1. Locate the existing API router / handler package via `so graph query "api router"`.
2. Match existing auth, validation, and error patterns.
3. Add tests next to similar handlers.
4. Update docs if public API surface changes.
5. Run lint/tests before finishing.
