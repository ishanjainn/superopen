# Superopen tracking policy: configuration below is committed; generated graph, session, memory, and audit state stays local.
# Updated by so init when the tracking policy changes; everything ignored below is derived or machine-local.

graph/
.graph-v2-*
graph.previous/
graph.failed/
sessions/
memory/
audit/
