# Generated by so init (LLM upgrade).
checks:
  - go_test_race_count_1
  - go_vet
  - web_test
  - web_typecheck
  - web_lint
  - plugin_marketplace_sync
  - behavior_tests
  - user_docs_updated
  - pr_title_convention

agent_rules:
  - "Keep modifications scoped to the requested change."
  - "Do not expose or commit secrets."
  - "Run the relevant Go, web, or plugin validation and report results."
  - "Cover behavior changes with tests and update user-facing documentation when applicable."
  - "Use established package boundaries and Conventional Commit-style PR titles."

judge_rubric: |
  Score sessions by whether they correctly located the owning CLI, internal, SDK, web, or plugin surface; delivered a focused implementation; preserved sensitive data; added appropriate tests and documentation; and ran or clearly reported relevant Go, web, and plugin checks. Penalize unrelated churn, skipped validation without explanation, and plugin changes that leave generated marketplace content unsynchronized.

sources:
  - /Users/ishanjain/work/superopen/CONTRIBUTING.md
