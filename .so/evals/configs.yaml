# Generated by so init (LLM upgrade).
checks:
  - tests
  - lint
  - go_build
  - race_patterns
  - pr_title_convention
  - typecheck

agent_rules:
  - "Prefer so graph query and AGENTS.md / .so/guardrails before broad Grep/Glob."
  - "Keep PRs focused with Conventional Commit titles."
  - "Run Go race/vet or web test/typecheck/lint for the area changed."
  - "Never introduce secrets or commit credentials."
  - "Sync plugins with scripts/sync-plugins.sh when marketplace templates change."
  - "Prune obsolete agent guidance when updating docs—do not only append."

judge_rubric: |
  Score sessions on whether the agent used Superopen harness context (graph, AGENTS.md, guardrails) before blind exploration; kept scope tight; ran stack-appropriate checks (Go race/vet or web test/typecheck/lint); used Conventional Commit style when committing/PRing; avoided secrets; and updated harness docs by pruning rather than only appending. Penalize unrelated churn, skipped tests on behavior changes, and inventing fake paths or credentials.

sources:
  - /Users/ishanjain/work/superopen/CONTRIBUTING.md
