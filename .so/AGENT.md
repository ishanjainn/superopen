# Superopen project context

Start with `.so/knowledge/architecture.md` for repository orientation and `.so/knowledge/conventions.md` for development workflow. Follow `.so/guardrails/guardrails.yaml`, use `.so/graph/graph.json` through `so graph query` before broad searching, and run the validation that matches the changed Go, web, or plugin surface.
