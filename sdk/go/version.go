package sdk

// Version is the current version of the Superopen Go SDK.
// This is the single source of truth - update this when cutting a release,
// then tag the commit as sdk/go/v<Version> to trigger the release workflow.
const Version = "0.2.0"
