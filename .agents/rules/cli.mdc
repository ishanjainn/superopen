---
description: so CLI and AXI output helpers
globs: cmd/so/**,internal/cli/**/*
alwaysApply: false
---

Read @cmd/so/AGENTS.md before changing commands, flags, exit codes, or default output shape.

Graph success is compact `NODE`/`EDGE`. Memory and sessions are **axi.md** TOON + dashboards + `help[]`. `so sessions hook` is Hidden host argv: stdout is host JSON, never AXI.

