---
description: Tests and Superopen benchmark harness
globs: **/*_test.go,scripts/**/*,benchmarks/**/*
alwaysApply: false
---

Read @scripts/AGENTS.md for package test commands. Eval harness: @benchmarks/AGENTS.md.

Never overwrite existing `benchmarks/results/*` stamp dirs. Headline scores are
Claude Code or OpenCode sessions only. Do not add benchmark-only product hacks.
Default `--scale small` (valid gate); `--scale full` to publish.
