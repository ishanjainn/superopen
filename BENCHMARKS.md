# Superopen Benchmarks

Last updated: 2026-08-28. Generated by `python3 benchmarks/run.py`.

## System

| | |
|---|---|
| Machine | MacBookPro · Apple M1 Pro · 8-core arm64 · 16 GB |
| OS | macOS 26.5.2 (darwin) |
| Isolate | docker (`so-bench` image; host HOME not mounted) |
| Superopen | locally built CLI bind-mounted at `/usr/local/bin/so` |
| Agent | Claude Code 2.1.241 · `claude-sonnet-5` (claude-code) |
| Scale | full (LOCOMO n=100, LME n=50, compare 6, graph 12) |

## Run

| Metric | Value | Detail |
|---|---|---|
| Duration | 40m 27s | graph 2m 35s; compare 13m 24s; memory_longmemeval 16m 47s; memory 24m 27s |
| Total cost | $3.16 | spend ledger |
| Graph index | 2m 31s | `so init`, $0 LLM |

## Results at a glance

| Suite | Dataset | Metric | Score |
|---|---|---|---|
| Graph | Django 5.2.4 | index time | 2m 31s |
| Graph | Django 5.2.4 | index size | 52,355 nodes, 346,672 edges, 4,652 files |
| Graph | Django 5.2.4 | 12-probe score | 12/12 |
| Graph | Django 5.2.4 | probe latency | p50=110 ms, p95=454 ms |
| Memory | LOCOMO (n=300 asked, 298 scored) | QA accuracy | 31.2% (key-fact coverage); judge 88%; 5/16 scored; qa_n=20; 4 empty-gold skipped; claude-code |
| Memory | LOCOMO (n=300 asked, 298 scored) | recall@10 | 90.6% (270/298) |
| Memory | LOCOMO (n=300 asked, 298 scored) | recall@5 | 86.6% (258/298) |
| Memory | LongMemEval-S (50) | QA accuracy | 20.0% (key-fact coverage); judge 30%; 4/20 scored; qa_n=20; claude-code |
| Memory | LongMemEval-S (50) | recall@10 | 82.0% (41/50) |
| Memory | LongMemEval-S (50) | recall@5 | 72.0% (36/50) |
| Memory | Contradiction | Rescue@10 | pending |
| Memory | Contradiction | historical-verbatim | pending |
| Cost | memory ingest | USD | $0.00; so-bge-small-en-v1.5-384; worker=bge |
| Cost | graph build | LLM credits | $0 |
| Session | Django (6) | duration | 13m 24s |
| Session | Django (6) | key-fact coverage | 1.00 (native 1.00) |
| Session | Django (6) | USD | $0.74 vs $0.91 native; 18% cheaper; cheaper on 4/6 questions |
| Session | Django (6) | cache_read tokens | 6,383,336 vs 7,423,318 native (14% less cache_read) |
| Latency | local fixture | `so memory search` | pending |

## Datasets

| Dataset | What we score | Notes |
|---|---|---|
| LOCOMO (`locomo10.json`) | 100 category-stratified QA (small); 300 on `--scale full` | File has 300 items |
| LongMemEval-S | 50 English questions | Haystack is not shrunk |
| Django | tag `5.2.4` | Graph probes + native vs Superopen sessions |
| Contradiction | Rescue@10, historical-verbatim | Go tests in `internal/memory/` |

## Conversational memory

### LOCOMO

| Adapter | recall@5 | recall@10 |
|---|---|---|
| Superopen (`so memory recall`) | 86.6% (258/298) | 90.6% (270/298) |
| BM25 | 84.6% (252/298) | 91.6% (273/298) |
| bow (bag-of-words) | 37.9% (113/298) | 54.0% (161/298) |
| RRF | 74.5% (222/298) | 90.3% (269/298) |

QA accuracy: 31.2% (key-fact coverage); judge 88%; 5/16 scored; qa_n=20; 4 empty-gold skipped; claude-code.

```bash
python3 benchmarks/run.py --mode memory --phase 2 --split locomo --scale small --so-bin ./bin/so
python3 benchmarks/run.py --mode memory --phase 3 --split locomo --scale small --qa-n 20 --host claude-code --model claude-sonnet-5 --max-spend 15 --so-bin ./bin/so
```

Asked n=300. Items without a mappable gold episode id are not scored.
Gold-in-store: hard_miss=0, rank_miss=28 of scored=298 (stored_docs=271). hard_miss = gold never captured; rank_miss = captured but not in top-10. BM25/bow/RRF search raw docs, Superopen searches the post-capture store.

### LongMemEval-S

| Adapter | recall@5 | recall@10 |
|---|---|---|
| Superopen (`so memory recall`) | 72.0% (36/50) | 82.0% (41/50) |
| BM25 | 72.0% (36/50) | 76.0% (38/50) |
| bow (bag-of-words) | 6.0% (3/50) | 16.0% (8/50) |
| RRF | 58.0% (29/50) | 74.0% (37/50) |

QA accuracy: 20.0% (key-fact coverage); judge 30%; 4/20 scored; qa_n=20; claude-code.
Gold-in-store: hard_miss=0, rank_miss=9 of scored=50 (stored_docs=2365). hard_miss = gold never captured; rank_miss = captured but not in top-10. BM25/bow/RRF search raw docs, Superopen searches the post-capture store.

```bash
python3 benchmarks/run.py --mode memory --phase 2 --split longmemeval --scale small --so-bin ./bin/so
python3 benchmarks/run.py --mode memory --phase 3 --split longmemeval --scale small --qa-n 20 --host claude-code --model claude-sonnet-5 --max-spend 15 --so-bin ./bin/so
```

### Contradiction

| Metric | Superopen |
|---|---|
| Rescue@10 | pending |
| historical-verbatim | pending |

```bash
python3 benchmarks/run.py --mode contradict
```

## Code intelligence

### Graph-tools (Django, 12 probes)

Score: **12/12**. Probe latency: p50=110 ms, p95=454 ms. Index: 52,355 nodes, 346,672 edges, 4,652 files.

```bash
python3 benchmarks/run.py --mode graph --repo django --index-timeout 1800 --so-bin ./bin/so
```

### Native vs Superopen sessions (Django, 6)

key-fact coverage: **1.00 (native 1.00)**.

USD: **$0.74 vs $0.91 native; 18% cheaper; cheaper on 4/6 questions**.

cache_read: **6,383,336 vs 7,423,318 native (14% less cache_read)**.

```bash
python3 benchmarks/run.py --mode compare --scale small --host claude-code --model claude-sonnet-5 --max-spend 20 --so-bin ./bin/so
```

## Temporal (Django LTS)

pending

```bash
python3 benchmarks/run.py --mode temporal --repo django --so-bin ./bin/so
```

## Latency

pending

```bash
python3 benchmarks/run.py --mode latency --so-bin ./bin/so
```
